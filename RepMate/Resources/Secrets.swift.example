// Copy this file to Secrets.swift and fill in your real values.
// ⚠️ Secrets.swift is gitignored — NEVER commit it.
// The matching value must be set as APP_SECRET in your AWS Lambda environment variables.

enum Secrets {
    static let appSecretKey = "REPLACE_WITH_YOUR_APP_SECRET"
    
    /// Full Lambda URL string for the Protein API
    static let aiProteinEndpointString = "REPLACE_WITH_YOUR_AI_PROTEIN_LAMBDA_URL"
    
    /// Full Lambda URL string for the AI Plan API
    static let aiPlanEndpointString = "REPLACE_WITH_YOUR_AI_PLAN_LAMBDA_URL"
}
